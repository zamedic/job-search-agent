apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: job-search-agent-sessions
  namespace: marcarndt
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
